"""Markdown-image exfiltration probe + detector for garak.

Layout mirrors garak's own package structure so files can be dropped
directly into a garak install (see deploy.sh) or vendored into a
garak fork for upstream PR.
"""
